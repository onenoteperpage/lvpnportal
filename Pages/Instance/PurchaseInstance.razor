@page "/purchase-instance"

<div class="container-fluid">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Purchase License</h1>
    </div>

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-dark">Select License Period</h6>
        </div>
        <div class="card-body">
            <div class="row g-3">
                <div class="col-12">
                    <p class="lead">
                        Choose from 1, 3, 6, or 12-month flexible license periods. Transfer your VPN service as
                        needed during the license period for added flexibility. Get access to our top-notch VPN
                        service for the selected duration.
                    </p>
                    <p class="lead">
                        Maintain multiple VPN services concurrently with ease. Once a license expires,
                        the VPN service gracefully concludes, securing your information.</p>
                    <hr class="my-4">
                    <p class="lead">Thank you for choosing our VPN services. We're here to provide a reliable and secure online experience.</p>

                    <div class="col-12 text-center">
                        <!-- Purchase Buttons -->
                        <stripe-buy-button buy-button-id="buy_btn_1OJHmLKcwfnufCukdPi6RXB0"
                                           publishable-key="pk_test_51HiceVKcwfnufCukziNp1oruZ2nuPpARzfQlWISrKODNbE3ZcvfkVZFwO4DZWY4FwPwI5unnNBLvN0qOkpd89grY00gltyqH3r"
                                           customer-email="@email"
                                           client-reference-id="@usrId">
                        </stripe-buy-button>

                        <stripe-buy-button buy-button-id="buy_btn_1OJIBMKcwfnufCukuvYGzD8F"
                                           publishable-key="pk_test_51HiceVKcwfnufCukziNp1oruZ2nuPpARzfQlWISrKODNbE3ZcvfkVZFwO4DZWY4FwPwI5unnNBLvN0qOkpd89grY00gltyqH3r"
                                           customer-email="@email"
                                           client-reference-id="@usrId">
                        </stripe-buy-button>

                        <stripe-buy-button buy-button-id="buy_btn_1OJIDRKcwfnufCukWeHh9J3m"
                                           publishable-key="pk_test_51HiceVKcwfnufCukziNp1oruZ2nuPpARzfQlWISrKODNbE3ZcvfkVZFwO4DZWY4FwPwI5unnNBLvN0qOkpd89grY00gltyqH3r"
                                           customer-email="@email"
                                           client-reference-id="@usrId">
                        </stripe-buy-button>

                        <stripe-buy-button buy-button-id="buy_btn_1OJIF2KcwfnufCuk0uSGCcjN"
                                           publishable-key="pk_test_51HiceVKcwfnufCukziNp1oruZ2nuPpARzfQlWISrKODNbE3ZcvfkVZFwO4DZWY4FwPwI5unnNBLvN0qOkpd89grY00gltyqH3r"
                                           customer-email="@email"
                                           client-reference-id="@usrId">
                        </stripe-buy-button>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>

@code {
    [CascadingParameter]
    private Task<AuthenticationState>? authenticationState { get; set; }

    private string usrId = string.Empty;
    private string email = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        if (authenticationState is not null)
        {
            var state = await authenticationState;
            email = state?.User?.Identity?.Name ?? "UNKNOWN USER";
            usrId = EncryptionHelper.GenerateToken(email);
        }

        await base.OnInitializedAsync();
    }
}
